import { useMemo } from "react";
import { getPopoverModifiers } from "./getPopoverModifiers";
function usePopoverModifiers(props) {
  const {
    allowedAutoPlacements,
    arrow,
    arrowElement,
    boundaryElement,
    constrainSize,
    distance,
    fallbackPlacements,
    margins,
    matchReferenceWidth,
    preventOverflow,
    skidding,
    tether,
    tetherOffset
  } = props;
  return useMemo(() => getPopoverModifiers({
    allowedAutoPlacements,
    arrow,
    arrowElement,
    boundaryElement,
    constrainSize,
    distance,
    fallbackPlacements,
    margins,
    matchReferenceWidth,
    preventOverflow,
    skidding,
    tether,
    tetherOffset
  }), [
    allowedAutoPlacements,
    arrow,
    arrowElement,
    boundaryElement,
    constrainSize,
    distance,
    fallbackPlacements,
    margins,
    matchReferenceWidth,
    preventOverflow,
    skidding,
    tether,
    tetherOffset
  ]);
}
export {
  usePopoverModifiers
};
